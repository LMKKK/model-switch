## Summary

This change does not introduce new capabilities or modify existing spec-level requirements. It is a UI text improvement only.

**No new requirements** — No new spec files needed.

**No modified requirements** — This change touches only the prompt message text in the CLI, which is not a spec-level behavior change (the `initial` prefill behavior remains unchanged, only the visible hint text is removed).